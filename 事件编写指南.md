# LifeGame 事件编写指南

新事件统一写在 `content-events.js` 的事件包中。事件内容与核心引擎分离，新增事件时通常不需要修改 `app.js`。

## 基本格式

```js
{
  id: "skill_math_example_001",
  type: "random",
  title: "事件标题",
  text: "事件发生时展示的正文。",
  trigger: {
    source: "skill",
    skillId: "KNOWLEDGE.MATH.ROOT.001",
    interval: 8,
    chance: 0.25,
  },
  stages: ["teen"],
  conditions: [
    { type: "skill", id: "KNOWLEDGE.MATH.ROOT.001", level: 20 },
  ],
  repeat: {
    once: false,
    cooldown: 2,
    weight: 30,
  },
  choices: [
    {
      text: "选项文案",
      effects: [
        { type: "skillXp", id: "KNOWLEDGE.MATH.ROOT.001", amount: 30 },
        { type: "resource", key: "stamina", amount: -3 },
      ],
      result: "选择后的结果描述。",
    },
  ],
}
```

## 触发来源

| source | 用途 | 常用附加字段 |
| --- | --- | --- |
| `skill` | 技能挂机完成 | `skillId`、`interval`、`chance` |
| `social` | 与 NPC 完成交互 | `npcId`、`actionId`、`minFriendship`、`interval`、`chance` |
| `time` | 月份或人生阶段检查 | 后续内容包使用 |
| `resource` | 资源达到特定状态 | 配合 `conditions` |
| `item` | 获得或使用道具 | 后续内容包使用 |
| `career` | 工作、校招和晋升 | 后续内容包使用 |
| `manual` | 由其他事件主动加入队列 | 不参与随机触发 |

`interval: 8` 表示每完成 8 次对应行为才检查一次。`chance: 0.25` 表示本次检查有 25% 概率进入候选池。技能和社交挂机事件建议同时设置二者，避免事件过于频繁。

## 常用条件

```js
{ type: "skill", id: "技能ID", level: 30 }
{ type: "resource", key: "money", value: 100 }
{ type: "stage", id: "work" }
{ type: "event", id: "前置事件ID" }
{ type: "tag", id: "剧情标签" }
{ type: "age", age: 18 }
```

多个条件默认全部满足。需要满足任意一项时：

```js
{
  type: "anyOf",
  items: [
    { type: "skill", id: "技能A", level: 20 },
    { type: "event", id: "事件B" },
  ],
}
```

## 标准效果

```js
{ type: "resource", key: "money", amount: 20 }
{ type: "attr", key: "discipline", amount: 1 }
{ type: "skillXp", id: "技能ID", amount: 30 }
{ type: "skillExposure", id: "隐藏技能ID", amount: 10, threshold: 60 }
{ type: "unlockSkill", id: "技能ID" }
{ type: "trainingAccess", id: "技能ID" }
{ type: "staminaMax", amount: 10 }
{ type: "item", id: "道具ID", count: 1 }
{ type: "tag", id: "剧情标签" }
```

- `skillXp`：增加已知技能经验；`amount` 是事件强度基准，系统会按当前等级所需经验同比放大，避免后期事件奖励失去意义。普通技能也可以借此在明确的发现事件中直接解锁。
- `skillExposure`：技能未发现时积累隐藏经验，达到 `threshold` 才显示；发现后继续作为普通经验增长。
- `trainingAccess`：开放技能的主动挂机权限，适合职业培训、课程或特殊道具。
- `staminaMax`：永久提高体力上限，并同步增加当前体力。

## 社交事件示例

```js
trigger: {
  source: "social",
  npcId: "teacher",
  actionId: "talk",
  minFriendship: 20,
  interval: 4,
  chance: 0.4,
}
```

## 命名与检查

- ID 使用小写英文和下划线，并以内容来源开头，例如 `skill_math_...`、`social_father_...`。
- 已发布事件不要修改 ID；存档使用 ID 记录完成状态和冷却时间。
- 重复事件可以修改文字和数值，但不要把 `once` 从 `false` 随意改为 `true`。
- 技能等级统一为 1–99；推荐把 20、40、60、80、99 作为入门、熟练、进阶、专家、精通里程碑。
- 完成编辑后运行 `node smoke-test.js` 和 `node balance-test.js`。前者检查内容与玩法流程，后者检查全技能 99 级的时间底线。
